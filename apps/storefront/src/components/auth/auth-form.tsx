'use client';

import {FormEvent, useState} from 'react';
import {useTranslations} from 'next-intl';
import {Link, useRouter} from '@/i18n/routing';
import {z} from 'zod';
import toast from 'react-hot-toast';
import {authService} from '@/services/api-service';

type AuthMode = 'login' | 'register';
type FormValues = {name: string; email: string; password: string; confirmPassword: string};
type Field = keyof FormValues;

export default function AuthForm({mode}: {mode: AuthMode}) {
  const t = useTranslations('Auth');
  const router = useRouter();
  const [values, setValues] = useState<FormValues>({name: '', email: '', password: '', confirmPassword: ''});
  const [errors, setErrors] = useState<Partial<Record<Field, string>>>({});
  const [loading, setLoading] = useState(false);
  const isRegister = mode === 'register';
  const update = (field: Field, value: string) => {setValues((current) => ({...current, [field]: value})); setErrors((current) => ({...current, [field]: undefined}));};
  const validate = () => {const base = {email: z.string().trim().min(1, t('errors.emailRequired')).email(t('errors.emailInvalid')), password: z.string().min(1, t('errors.passwordRequired')).min(8, t('errors.passwordShort'))}; const schema = isRegister ? z.object({...base, name: z.string().trim().min(1, t('errors.nameRequired')), confirmPassword: z.string().min(1, t('errors.confirmRequired'))}).refine((data) => data.password === data.confirmPassword, {path: ['confirmPassword'], message: t('errors.passwordMismatch')}) : z.object(base); const result = schema.safeParse(values); if (result.success) return {}; const next: Partial<Record<Field, string>> = {}; result.error.issues.forEach((issue) => {const field = issue.path[0] as Field; if (!next[field]) next[field] = issue.message;}); return next;};
  const submit = async (event: FormEvent<HTMLFormElement>) => {event.preventDefault(); const next = validate(); setErrors(next); if (Object.keys(next).length) {toast.error(t('errors.fixForm')); return;} setLoading(true); try {await (isRegister ? authService.register(values) : authService.login(values)); toast.success(t(`${mode}.success`)); router.push(isRegister ? '/login' : '/');} catch {toast.error(t('errors.generic'));} finally {setLoading(false);}};
  const inputClass = (field: Field) => `mt-2 block min-h-12 w-full rounded-xl border bg-white px-4 text-[15px] text-slate-900 outline-none transition placeholder:text-slate-400 focus:ring-4 ${errors[field] ? 'border-rose-400 focus:border-rose-500 focus:ring-rose-100' : 'border-slate-200 focus:border-indigo-500 focus:ring-indigo-100'}`;
  const fieldError = (field: Field) => errors[field] ? <span className="mt-1.5 block text-xs text-rose-600">{errors[field]}</span> : null;
  return <section className="flex w-full max-w-xl flex-col justify-center px-5 py-10 sm:px-10 sm:py-14 lg:px-16 xl:px-24"><div className="mb-10 flex items-center gap-3 lg:hidden"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-slate-950 text-sm font-semibold text-white">R</span><span className="font-semibold tracking-tight text-slate-950">{t('visual.brand')}</span></div><div className="mb-8"><p className="mb-3 text-sm font-semibold uppercase tracking-[0.2em] text-indigo-600">{t(`${mode}.eyebrow`)}</p><h1 className="text-3xl font-semibold tracking-[-0.04em] text-slate-950 sm:text-4xl">{t(`${mode}.title`)}</h1><p className="mt-3 max-w-md text-[15px] leading-7 text-slate-500">{t(`${mode}.description`)}</p></div><form className="space-y-5" onSubmit={submit} noValidate>{isRegister && <label className="block text-sm font-medium text-slate-700">{t('fields.name')}<input className={inputClass('name')} value={values.name} onChange={(e) => update('name', e.target.value)} placeholder={t('placeholders.name')} autoComplete="name" aria-invalid={Boolean(errors.name)} />{fieldError('name')}</label>}<label className="block text-sm font-medium text-slate-700">{t('fields.email')}<input className={inputClass('email')} value={values.email} onChange={(e) => update('email', e.target.value)} placeholder={t('placeholders.email')} type="email" autoComplete="email" aria-invalid={Boolean(errors.email)} />{fieldError('email')}</label><label className="block text-sm font-medium text-slate-700">{t('fields.password')}<input className={inputClass('password')} value={values.password} onChange={(e) => update('password', e.target.value)} placeholder={t('placeholders.password')} type="password" minLength={8} autoComplete={isRegister ? 'new-password' : 'current-password'} aria-invalid={Boolean(errors.password)} />{fieldError('password')}</label>{isRegister && <label className="block text-sm font-medium text-slate-700">{t('fields.confirmPassword')}<input className={inputClass('confirmPassword')} value={values.confirmPassword} onChange={(e) => update('confirmPassword', e.target.value)} placeholder={t('placeholders.confirmPassword')} type="password" minLength={8} autoComplete="new-password" aria-invalid={Boolean(errors.confirmPassword)} />{fieldError('confirmPassword')}</label>}{!isRegister && <div className="flex justify-end"><Link href="/forgot-password" className="text-sm font-medium text-indigo-600 transition hover:text-violet-600">{t('login.forgotPassword')}</Link></div>}<button type="submit" disabled={loading} className="group flex min-h-12 w-full items-center justify-center gap-2 rounded-xl bg-slate-950 px-5 text-sm font-semibold text-white shadow-lg shadow-slate-950/10 transition hover:bg-indigo-700 disabled:cursor-not-allowed disabled:opacity-60">{loading && <span className="h-4 w-4 animate-spin rounded-full border-2 border-white/35 border-t-white" aria-hidden="true" />}{loading ? t('loading') : t(`${mode}.submit`)}{!loading && <span aria-hidden="true" className="text-lg transition-transform group-hover:translate-x-1">→</span>}</button></form><p className="mt-8 text-center text-sm text-slate-500">{t(`${mode}.switchPrompt`)}{' '}<Link className="font-semibold text-indigo-600 hover:text-violet-600" href={isRegister ? '/login' : '/register'}>{t(`${mode}.switchAction`)}</Link></p></section>;
}
