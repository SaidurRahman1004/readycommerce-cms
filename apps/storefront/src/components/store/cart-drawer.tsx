'use client';
import Image from 'next/image';
import {useTranslations} from 'next-intl';
import {Link} from '@/i18n/routing';
import {useCart} from './cart-context';
export default function CartDrawer() { const t = useTranslations('Storefront'); const {items, subtotal, isOpen, closeCart, updateQuantity, removeItem} = useCart(); if (!isOpen) return null; return <div className="fixed inset-0 z-50 bg-foreground/30 backdrop-blur-sm" onClick={closeCart}><aside role="dialog" aria-modal="true" className="absolute right-0 top-0 flex h-full w-full max-w-md flex-col bg-background shadow-premium" onClick={(event) => event.stopPropagation()}><div className="flex items-center justify-between border-b border-border px-5 py-6 sm:px-8"><div><p className="text-[13px] font-bold uppercase tracking-[0.2em] text-primary">{t('cart.eyebrow')}</p><h2 className="mt-1.5 text-2xl font-bold">{t('cart.title')}</h2></div><button type="button" onClick={closeCart} className="flex h-11 w-11 items-center justify-center rounded-full bg-muted text-xl" aria-label={t('cart.close')}>×</button></div><div className="flex-1 overflow-y-auto px-5 py-6 sm:px-8">{items.length === 0 ? <div className="flex h-full flex-col items-center justify-center text-center"><h3 className="text-xl font-bold">{t('cart.emptyTitle')}</h3><p className="mt-3 max-w-[260px] text-[15px] text-muted-foreground">{t('cart.emptyDescription')}</p><Link href="/shop" onClick={closeCart} className="mt-8 rounded-xl bg-primary px-8 py-3.5 font-bold text-white">{t('cart.continue')}</Link></div> : <div className="space-y-7">{items.map((item) => <div key={item.productId} className="flex gap-4"><div className="relative h-28 w-24 shrink-0 overflow-hidden rounded-xl bg-muted">{item.image && <Image src={item.image} alt={item.name || ''} fill sizes="96px" className="object-cover" />}</div><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><div><h3 className="text-[15px] font-bold">{item.name || item.productId}</h3><p className="mt-1 text-sm font-medium text-muted-foreground">৳{(item.price || 0).toLocaleString()}</p></div><button type="button" onClick={() => removeItem(item.productId)} className="text-[13px] font-bold text-muted-foreground">{t('cart.remove')}</button></div><div className="mt-5 flex h-10 w-fit items-center rounded-xl border border-border"><button type="button" onClick={() => updateQuantity(item.productId, item.quantity - 1)} className="flex h-full w-10 items-center justify-center">−</button><span className="w-8 text-center text-sm font-bold">{item.quantity}</span><button type="button" onClick={() => updateQuantity(item.productId, item.quantity + 1)} className="flex h-full w-10 items-center justify-center">+</button></div></div></div>)}</div>}</div>{items.length > 0 && <div className="border-t border-border bg-surface px-5 py-6 sm:px-8"><div className="mb-5 flex items-center justify-between text-[17px] font-bold"><span>{t('cart.subtotal')}</span><span>৳{subtotal.toLocaleString()}</span></div><Link href="/checkout" onClick={closeCart} className="flex min-h-14 items-center justify-center rounded-xl bg-primary font-bold text-white">{t('cart.checkout')}</Link></div>}</aside></div>; }
