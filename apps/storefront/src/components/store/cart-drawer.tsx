'use client';
import Image from 'next/image';
import {useTranslations} from 'next-intl';
import {Link} from '@/i18n/routing';
import {useCart} from './cart-context';

function CloseIcon() { return <svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" className="h-5 w-5"><path d="M18 6L6 18M6 6l12 12" strokeLinecap="round" strokeLinejoin="round"/></svg>; }
function EmptyCartIcon() { return <svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1" className="h-16 w-16 text-muted-foreground/30"><circle cx="9" cy="21" r="1"></circle><circle cx="20" cy="21" r="1"></circle><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path></svg>; }

export default function CartDrawer() {
  const t = useTranslations('Storefront');
  const {items, subtotal, isOpen, closeCart, updateQuantity, removeItem, pendingItem} = useCart();
  if (!isOpen) return null;
  return <div className="fixed inset-0 z-50 bg-foreground/40 backdrop-blur-md" onClick={closeCart}><aside role="dialog" aria-modal="true" className="absolute right-0 top-0 flex h-full w-full max-w-md flex-col bg-background shadow-premium" onClick={(event) => event.stopPropagation()}><div className="flex items-center justify-between border-b border-border px-5 py-6 sm:px-8"><div><p className="text-[13px] font-bold uppercase tracking-[0.2em] text-primary">{t('cart.eyebrow')}</p><h2 className="mt-1.5 text-2xl font-bold">{t('cart.title')}</h2></div><button type="button" onClick={closeCart} className="flex h-11 w-11 items-center justify-center rounded-full bg-muted/50 text-foreground transition-all hover:bg-muted" aria-label={t('cart.close')}><CloseIcon /></button></div><div className="flex-1 overflow-y-auto px-5 py-6 sm:px-8">{items.length === 0 ? <div className="flex h-full flex-col items-center justify-center text-center"><div className="mb-6 flex h-32 w-32 items-center justify-center rounded-full bg-muted/50"><EmptyCartIcon /></div><h3 className="text-2xl font-bold text-foreground">{t('cart.emptyTitle')}</h3><p className="mt-4 max-w-[280px] text-[15px] leading-relaxed text-muted-foreground">{t('cart.emptyDescription')}</p><Link href="/shop" onClick={closeCart} className="mt-10 min-h-14 flex items-center justify-center rounded-full bg-primary px-10 font-bold text-white shadow-lg transition-transform hover:scale-105">{t('cart.continue')}</Link></div> : <div className="space-y-7">{items.map((item) => { const busy = pendingItem === item.productId; return <div key={item.productId} className="flex gap-4"><div className="relative h-28 w-24 shrink-0 overflow-hidden rounded-xl bg-muted">{item.image && <Image src={item.image} alt={item.name || ''} fill sizes="96px" className="object-cover" />}</div><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><div><h3 className="text-[15px] font-bold">{item.name || item.productId}</h3><p className="mt-1 text-sm font-medium text-muted-foreground">৳{(item.price || 0).toLocaleString()}</p></div><button type="button" disabled={busy} onClick={() => void removeItem(item.productId)} className="text-[13px] font-bold text-muted-foreground disabled:opacity-50 transition-colors hover:text-red-500">{t('cart.remove')}</button></div><div className="mt-5 flex h-10 w-fit items-center rounded-xl border border-border bg-surface"><button type="button" disabled={busy} onClick={() => void updateQuantity(item.productId, item.quantity - 1)} className="flex h-full w-10 items-center justify-center disabled:opacity-50 transition-colors hover:text-primary">{busy ? <span className="h-4 w-4 animate-spin rounded-full border-2 border-primary/30 border-t-primary" /> : '-'}</button><span className="w-8 text-center text-sm font-bold">{item.quantity}</span><button type="button" disabled={busy} onClick={() => void updateQuantity(item.productId, item.quantity + 1)} className="flex h-full w-10 items-center justify-center disabled:opacity-50 transition-colors hover:text-primary">+</button></div></div></div>; })}</div>}</div>{items.length > 0 && <div className="border-t border-border bg-surface px-5 py-6 sm:px-8"><div className="mb-6 flex items-center justify-between text-lg font-bold"><span>{t('cart.subtotal')}</span><span>৳{subtotal.toLocaleString()}</span></div><Link href="/checkout" onClick={closeCart} className="flex min-h-14 items-center justify-center rounded-full bg-primary font-bold text-white shadow-lg transition-transform hover:scale-[1.02]">{t('cart.checkout')}</Link></div>}</aside></div>;
}
