'use client';

import Image from 'next/image';
import {useTranslations} from 'next-intl';
import {Link} from '@/i18n/routing';
import {catalog} from './catalog';
import {useCart} from './cart-context';

export default function CartDrawer() {
  const t = useTranslations('Storefront');
  const {items, subtotal, isOpen, closeCart, updateQuantity, removeItem} = useCart();
  if (!isOpen) return null;
  return <div className="fixed inset-0 z-50 bg-slate-950/35" onClick={closeCart}><aside role="dialog" aria-modal="true" aria-label={t('cart.title')} className="absolute right-0 top-0 flex h-full w-full max-w-md flex-col bg-white shadow-2xl" onClick={(event) => event.stopPropagation()}><div className="flex items-center justify-between border-b border-slate-200 px-5 py-5 sm:px-7"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-indigo-600">{t('cart.eyebrow')}</p><h2 className="mt-1 text-xl font-semibold tracking-tight">{t('cart.title')}</h2></div><button type="button" onClick={closeCart} className="flex h-10 w-10 items-center justify-center rounded-full bg-slate-100 text-xl text-slate-600 transition hover:bg-slate-200" aria-label={t('cart.close')}>×</button></div><div className="flex-1 overflow-y-auto px-5 py-5 sm:px-7">{items.length === 0 ? <div className="flex h-full flex-col items-center justify-center text-center"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-indigo-50 text-2xl text-indigo-600">⌁</div><h3 className="mt-5 text-lg font-semibold">{t('cart.emptyTitle')}</h3><p className="mt-2 max-w-xs text-sm leading-6 text-slate-500">{t('cart.emptyDescription')}</p><Link href="/shop" onClick={closeCart} className="mt-6 rounded-full bg-slate-950 px-5 py-3 text-sm font-semibold text-white">{t('cart.continue')}</Link></div> : <div className="space-y-6">{items.map((item) => { const product = catalog.find((entry) => entry.id === item.productId); if (!product) return null; return <div key={item.productId} className="flex gap-4"><div className="relative h-24 w-20 shrink-0 overflow-hidden rounded-xl bg-slate-100"><Image src={product.image} alt={t(`products.${product.key}.name`)} fill sizes="80px" className="object-cover" /></div><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><div><h3 className="text-sm font-semibold text-slate-900">{t(`products.${product.key}.name`)}</h3><p className="mt-1 text-xs text-slate-500">${product.price}</p></div><button type="button" onClick={() => removeItem(item.productId)} className="text-xs text-slate-400 hover:text-rose-600">{t('cart.remove')}</button></div><div className="mt-4 flex h-9 w-fit items-center rounded-lg border border-slate-200"><button type="button" onClick={() => updateQuantity(item.productId, item.quantity - 1)} className="h-full w-9 text-slate-500">−</button><span className="w-7 text-center text-xs font-semibold">{item.quantity}</span><button type="button" onClick={() => updateQuantity(item.productId, item.quantity + 1)} className="h-full w-9 text-slate-500">+</button></div></div></div>; })}</div>}</div>{items.length > 0 && <div className="border-t border-slate-200 px-5 py-5 sm:px-7"><div className="mb-4 flex items-center justify-between text-base font-semibold"><span>{t('cart.subtotal')}</span><span>${subtotal}</span></div><Link href="/checkout" onClick={closeCart} className="flex min-h-12 items-center justify-center rounded-xl bg-slate-950 text-sm font-semibold text-white transition hover:bg-indigo-700">{t('cart.checkout')}</Link><p className="mt-3 text-center text-xs text-slate-400">{t('cart.note')}</p></div>}</aside></div>;
}
