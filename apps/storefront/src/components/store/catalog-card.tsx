'use client';
import Image from 'next/image';
import {useTranslations} from 'next-intl';
import {Link} from '@/i18n/routing';
import {CatalogProduct} from '@/services/api-service';
import {useCart} from './cart-context';
import WishlistButton from './wishlist-button';
export default function CatalogCard({product}: {product: CatalogProduct}) { const t = useTranslations('Storefront'); const {addItem} = useCart(); const image = product.images[0]; const cartProduct = {price: product.basePrice, name: product.name, image}; return <article className="group flex flex-col rounded-[2rem] border border-border/60 bg-surface p-3 shadow-sm transition-all duration-500 hover:-translate-y-1.5 hover:shadow-premium"><div className="relative aspect-[4/5] overflow-hidden rounded-[1.5rem] bg-muted"><Link href={`/products/${product._id}`}><Image src={image} alt={product.name} fill sizes="(max-width: 640px) 45vw, (max-width: 1024px) 30vw, 23vw" className="object-cover transition-transform duration-1000 group-hover:scale-110" /></Link><WishlistButton productId={product._id} /><button type="button" onClick={() => addItem(product._id, 1, cartProduct)} className="absolute bottom-3 left-3 right-3 hidden min-h-12 rounded-xl bg-white/95 text-sm font-bold shadow-lg sm:block sm:translate-y-6 sm:opacity-0 sm:group-hover:translate-y-0 sm:group-hover:opacity-100">{t('products.addToCart')}</button></div><div className="flex flex-1 items-start justify-between gap-3 p-3 pt-5"><div className="min-w-0"><Link href={`/products/${product._id}`} className="line-clamp-2 text-[15px] font-bold tracking-tight hover:text-primary">{product.name}</Link><p className="mt-1.5 text-[13px] text-muted-foreground">{product.category.name}</p></div><p className="shrink-0 text-[17px] font-bold">৳{product.basePrice.toLocaleString()}</p></div></article>; }
