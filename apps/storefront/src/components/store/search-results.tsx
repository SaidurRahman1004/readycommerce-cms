'use client';
import {useEffect, useState} from 'react';
import {useTranslations} from 'next-intl';
import {useSearchParams} from 'next/navigation';
import {catalogService, CatalogProduct} from '@/services/api-service';
import CatalogCard from './catalog-card';
function Skeleton() { return <div className="animate-pulse rounded-2xl bg-muted"><div className="aspect-[4/5] rounded-2xl bg-border/50" /><div className="mt-4 h-4 w-2/3 rounded bg-border/50" /></div>; }
export default function SearchResults() { const t = useTranslations('Discovery'); const params = useSearchParams(); const query = params.get('q')?.trim() ?? ''; const [products, setProducts] = useState<CatalogProduct[]>([]); const [loading, setLoading] = useState(true); useEffect(() => {let active = true; const timer = window.setTimeout(() => {setLoading(true); catalogService.products({search: query, limit: 48}).then((result) => {if (active) setProducts(result.data);}).catch(() => {if (active) setProducts([]);}).finally(() => {if (active) setLoading(false);});}, 0); return () => {active = false; window.clearTimeout(timer);};}, [query]); return <main className="min-h-screen bg-[#fafbff] px-5 py-12 text-slate-950 sm:px-8 lg:px-10"><div className="mx-auto max-w-7xl"><p className="text-xs font-semibold uppercase tracking-[0.22em] text-indigo-600">{t('search.eyebrow')}</p><h1 className="mt-3 text-4xl font-semibold tracking-[-0.06em] sm:text-5xl">{query ? t('search.resultsFor', {query}) : t('search.title')}</h1><div className="mt-10">{loading ? <div className="grid grid-cols-2 gap-4 sm:gap-6 lg:grid-cols-4">{[1,2,3,4].map((item) => <Skeleton key={item} />)}</div> : products.length ? <div className="grid grid-cols-2 gap-x-4 gap-y-9 sm:gap-x-6 lg:grid-cols-3 xl:grid-cols-4">{products.map((product) => <CatalogCard key={product._id} product={product} />)}</div> : <div className="rounded-3xl border border-dashed border-slate-300 bg-white px-6 py-20 text-center"><p className="text-xl font-semibold">{t('search.emptyTitle')}</p><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-slate-500">{t('search.emptyDescription')}</p></div>}</div></div></main>; }
