'use client';

import Image from 'next/image';
import {useEffect, useState} from 'react';
import {useTranslations} from 'next-intl';
import {catalogService, CatalogProduct} from '@/services/api-service';
import {useCart} from './cart-context';
import WishlistButton from './wishlist-button';

function DetailSkeleton() { return <main className="mx-auto grid max-w-7xl animate-pulse gap-10 px-5 py-10 lg:grid-cols-2 lg:px-10 lg:py-16"><div className="aspect-square rounded-[2rem] bg-muted" /><div className="space-y-5 py-8"><div className="h-5 w-1/3 rounded bg-muted" /><div className="h-14 w-4/5 rounded bg-muted" /><div className="h-8 w-1/4 rounded bg-muted" /><div className="h-32 rounded bg-muted" /></div></main>; }
export default function ProductDetail({productId}: {productId: string}) { const t = useTranslations('Storefront'); const {addItem} = useCart(); const [product, setProduct] = useState<CatalogProduct | null>(null); const [loading, setLoading] = useState(true); const [error, setError] = useState(false); const [active, setActive] = useState(0); const [quantity, setQuantity] = useState(1); const [variant, setVariant] = useState(''); const [open, setOpen] = useState('description'); useEffect(() => {catalogService.product(productId).then((result) => {setProduct(result.data); setVariant(result.data.variants[0]?._id || '');}).catch(() => setError(true)).finally(() => setLoading(false));}, [productId]); if (loading) return <DetailSkeleton />; if (error || !product) return <main className="mx-auto min-h-[60vh] max-w-3xl px-5 py-20 text-center"><h1 className="text-2xl font-bold">{t('shop.empty')}</h1></main>; const gallery = product.images.length ? product.images : []; const selected = product.variants.find((item) => item._id === variant) || product.variants[0]; const stock = selected?.stock ?? 0; const add = () => {if (selected && stock > 0) addItem(product._id, quantity);}; return <main className="bg-background pb-24 text-foreground lg:pb-16"><div className="mx-auto grid max-w-7xl gap-10 px-5 py-8 sm:px-8 lg:grid-cols-2 lg:gap-16 lg:px-10 lg:py-16"><section><div className="relative"><div className="flex snap-x snap-mandatory gap-4 overflow-x-auto scroll-smooth lg:block lg:overflow-hidden" onScroll={(event) => {const element = event.currentTarget; if (element.clientWidth) setActive(Math.round(element.scrollLeft / element.clientWidth));}}>{gallery.map((image, index) => <div key={image} className={`relative min-w-full snap-center aspect-[4/5] overflow-hidden rounded-[2rem] bg-muted lg:aspect-square ${index === active ? 'lg:block' : 'lg:hidden'}`}><Image src={image} alt={product.name} fill priority={index === 0} sizes="(max-width: 1023px) 90vw, 50vw" className="object-cover" /></div>)}</div><WishlistButton productId={product._id} /></div><div className="mt-5 flex gap-4 overflow-x-auto pb-2">{gallery.map((image, index) => <button type="button" key={image} onClick={() => setActive(index)} className={`relative aspect-square w-24 shrink-0 overflow-hidden rounded-[1.25rem] border-2 ${active === index ? 'border-primary ring-2 ring-primary-subtle' : 'border-transparent opacity-70'}`} aria-label={t('pdp.thumbnail', {number: index + 1})}><Image src={image} alt="" fill sizes="96px" className="object-cover" /></button>)}</div></section><section className="flex flex-col justify-center"><p className="text-[13px] font-bold uppercase tracking-[0.25em] text-primary">{product.category.name}</p><h1 className="mt-5 text-4xl font-bold tracking-tight sm:text-5xl">{product.name}</h1><div className="mt-6 flex items-center gap-3"><span className="text-[15px] tracking-widest text-amber-500" aria-label={t('pdp.rating')}>★★★★★</span><span className="text-[14px] font-medium text-muted-foreground">{t('pdp.reviews')}</span></div><p className="mt-8 text-3xl font-bold">৳{(selected?.price || product.basePrice).toLocaleString()}</p><p className="mt-6 max-w-lg text-[16px] leading-relaxed text-muted-foreground">{product.description || product.shortDescription}</p><div className="mt-10"><p className="text-[14px] font-bold">{t('pdp.size')}</p><div className="mt-4 flex flex-wrap gap-3">{product.variants.map((item) => <button type="button" key={item._id} onClick={() => setVariant(item._id)} className={`rounded-full border px-5 py-3 text-[14px] font-bold ${variant === item._id ? 'border-primary bg-primary text-white' : 'border-border text-muted-foreground'}`}>{item.name}</button>)}</div></div><div className="mt-10 flex gap-4"><div className="flex h-14 w-32 items-center justify-between rounded-xl border border-border bg-surface px-1"><button type="button" aria-label={t('pdp.decrease')} onClick={() => setQuantity((value) => Math.max(1, value - 1))} className="flex h-10 w-10 items-center justify-center text-lg">−</button><span className="w-8 text-center text-[15px] font-bold">{quantity}</span><button type="button" aria-label={t('pdp.increase')} onClick={() => setQuantity((value) => Math.min(stock || 1, value + 1))} className="flex h-10 w-10 items-center justify-center text-lg">+</button></div><button type="button" disabled={!stock} onClick={add} className="hidden h-14 flex-1 rounded-xl bg-primary px-6 text-[15px] font-bold text-white shadow-premium transition-all hover:-translate-y-0.5 disabled:cursor-not-allowed disabled:opacity-50 sm:block">{stock ? t('products.addToCart') : 'Out of stock'}</button></div><div className="mt-12 divide-y divide-border border-y border-border">{['description', 'shipping'].map((key) => <div key={key}><button type="button" onClick={() => setOpen(open === key ? '' : key)} className="flex min-h-[64px] w-full items-center justify-between text-left text-[15px] font-bold hover:text-primary">{t(`pdp.${key}.title`)}<span className="text-xl text-muted-foreground">{open === key ? '−' : '+'}</span></button>{open === key && <p className="pb-6 pr-8 text-[15px] leading-relaxed text-muted-foreground">{key === 'description' ? product.description : t('pdp.shipping.body')}</p>}</div>)}</div></section></div><div className="fixed inset-x-0 bottom-0 z-30 border-t border-border bg-background/90 p-4 shadow-[0_-4px_20px_rgba(0,0,0,0.05)] backdrop-blur-xl sm:hidden"><button type="button" disabled={!stock} onClick={add} className="h-14 w-full rounded-xl bg-primary text-[15px] font-bold text-white shadow-premium disabled:opacity-50">{stock ? `${t('products.addToCart')} · ৳${(selected?.price || product.basePrice) * quantity}` : 'Out of stock'}</button></div></main>; }
