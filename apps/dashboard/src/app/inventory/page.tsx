'use client';
import { useCallback,useEffect,useState } from 'react'; import toast from 'react-hot-toast'; import { adminCatalogService,type AdminInventory } from '../../services/api-service'; import { Breadcrumbs,PageHeader } from '../../components/ui/page-header'; import { EmptyState,ErrorState,Skeleton } from '../../components/ui/primitives';
export default function InventoryPage(){const [items,setItems]=useState<AdminInventory[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState(false);const load=useCallback(()=>{setLoading(true);adminCatalogService.inventory().then(r=>setItems(r.data)).catch(()=>setError(true)).finally(()=>setLoading(false))},[]);useEffect(()=>{const t=window.setTimeout(load,0);return()=>window.clearTimeout(t)},[load]);const adjust=async(item:AdminInventory)=>{const value=window.prompt('Set stock quantity',String(item.quantity));if(value===null)return;const quantity=Number(value);if(!Number.isInteger(quantity)||quantity<0)return toast.error('Enter a non-negative whole number.');try{await adminCatalogService.adjust(item._id,quantity);toast.success('Stock updated.');load()}catch(e){toast.error(e instanceof Error?e.message:'Could not update stock.')}};return <section className="mx-auto max-w-7xl"><Breadcrumbs items={[{label:'Inventory'}]}/><PageHeader eyebrow="Operations" title="Inventory" description="Monitor real-time availability and make safe stock adjustments."/>{loading?<div className="space-y-3">{[1,2,3].map(x=><Skeleton className="h-20" key={x}/>)}</div>:error?<ErrorState title="Inventory could not be loaded." retry={load}/>:!items.length?<EmptyState title="No inventory records." description="Inventory appears when product variants are created."/>:<div className="overflow-x-auto rounded-2xl border border-border bg-white/85"><table className="w-full min-w-[720px] text-left text-sm"><thead className="border-b border-border text-xs uppercase text-slate-500"><tr>{['Product','SKU','Available','Reserved','Threshold','Status','Action'].map(x=><th className="px-5 py-4" key={x}>{x}</th>)}</tr></thead><tbody className="divide-y divide-border">{items.map(i=><tr key={i._id} className="hover:bg-muted/50"><td className="px-5 py-4 font-bold">{i.product}<span className="block text-xs font-normal text-slate-500">{i.variant}</span></td><td className="px-5 py-4">{i.sku}</td><td className="px-5 py-4 font-bold">{i.available}</td><td className="px-5 py-4">{i.reservedQuantity}</td><td className="px-5 py-4">{i.threshold}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${i.status==='out_of_stock'?'bg-rose-50 text-rose-700':i.status==='low_stock'?'bg-amber-50 text-amber-700':'bg-emerald-50 text-emerald-700'}`}>{i.status.replaceAll('_',' ')}</span></td><td className="px-5 py-4"><button onClick={()=>adjust(i)} className="font-bold text-primary">Adjust stock</button></td></tr>)}</tbody></table></div>}</section>}
