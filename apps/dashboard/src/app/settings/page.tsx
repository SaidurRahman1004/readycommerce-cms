'use client';
import { useEffect, useState } from 'react'; import toast from 'react-hot-toast'; import { Breadcrumbs,PageHeader } from '../../components/ui/page-header'; import { ErrorState,Skeleton } from '../../components/ui/primitives'; import { settingsService,type StoreSettings } from '../../services/settings-service';
const defaults:StoreSettings={storeName:'ReadyCommerce',contactEmail:'',supportPhone:'',currency:'BDT',insideDhakaRate:60,outsideDhakaRate:120};
export default function SettingsPage(){const [form,setForm]=useState(defaults);const [loading,setLoading]=useState(true);const [saving,setSaving]=useState(false);const [error,setError]=useState(false);useEffect(()=>{settingsService.get().then(r=>setForm({...defaults,...r.data})).catch(()=>setError(true)).finally(()=>setLoading(false))},[]);const set=(key:keyof StoreSettings,value:string)=>setForm(v=>({...v,[key]:key==='insideDhakaRate'||key==='outsideDhakaRate'?Number(value):value}));const save=async(e:React.FormEvent)=>{e.preventDefault();if(!form.storeName.trim())return toast.error('Store name is required.');if(form.insideDhakaRate<0||form.outsideDhakaRate<0)return toast.error('Shipping rates cannot be negative.');setSaving(true);try{const r=await settingsService.save(form);setForm(r.data);toast.success('Store settings saved.')}catch(x){toast.error(x instanceof Error?x.message:'Could not save settings.')}finally{setSaving(false)}};if(loading)return <Skeleton className="h-96"/>;if(error)return <ErrorState title="Settings could not be loaded." retry={()=>window.location.reload()}/>;return <section className="mx-auto max-w-4xl"><Breadcrumbs items={[{label:'Settings'}]}/><PageHeader eyebrow="Configuration" title="Store settings" description="Manage the global identity, contact information and delivery rates for your store."/><form onSubmit={save} className="grid gap-5 rounded-2xl border border-border bg-white/85 p-5 shadow-sm sm:p-7"><div className="grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-sm font-semibold">Store name<input value={form.storeName} onChange={e=>set('storeName',e.target.value)} className="min-h-11 rounded-xl border border-border px-3 outline-none focus:border-primary" /></label><label className="grid gap-2 text-sm font-semibold">Contact email<input type="email" value={form.contactEmail} onChange={e=>set('contactEmail',e.target.value)} className="min-h-11 rounded-xl border border-border px-3 outline-none focus:border-primary" /></label><label className="grid gap-2 text-sm font-semibold">Support phone<input value={form.supportPhone} onChange={e=>set('supportPhone',e.target.value)} className="min-h-11 rounded-xl border border-border px-3 outline-none focus:border-primary" /></label><label className="grid gap-2 text-sm font-semibold">Currency<select value={form.currency} onChange={e=>set('currency',e.target.value)} className="min-h-11 rounded-xl border border-border px-3 outline-none focus:border-primary"><option value="BDT">BDT (৳)</option><option value="USD">USD ($)</option></select></label></div><div className="border-t border-border pt-5"><h2 className="font-bold">Shipping rates</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-sm font-semibold">Inside Dhaka<input type="number" min="0" value={form.insideDhakaRate} onChange={e=>set('insideDhakaRate',e.target.value)} className="min-h-11 rounded-xl border border-border px-3 outline-none focus:border-primary" /></label><label className="grid gap-2 text-sm font-semibold">Outside Dhaka<input type="number" min="0" value={form.outsideDhakaRate} onChange={e=>set('outsideDhakaRate',e.target.value)} className="min-h-11 rounded-xl border border-border px-3 outline-none focus:border-primary" /></label></div></div><div className="flex justify-end"><button disabled={saving} className="min-h-11 rounded-xl bg-primary px-6 py-3 text-sm font-bold text-white disabled:opacity-60">{saving?'Saving…':'Save settings'}</button></div></form></section>}
