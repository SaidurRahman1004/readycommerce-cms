'use client';
import { useState } from 'react';
const links = [{ label: 'Overview', icon: '⌂' }, { label: 'Orders', icon: '↗' }, { label: 'Products', icon: '◈' }, { label: 'Customers', icon: '◎' }, { label: 'Settings', icon: '⚙' }];
export default function DashboardShell({ children }: { children: React.ReactNode }) { const [open, setOpen] = useState(false); return <div className="min-h-screen md:flex"><aside className={`${open ? 'translate-x-0' : '-translate-x-full'} fixed inset-y-0 left-0 z-40 w-72 border-r border-border bg-white/90 p-6 shadow-xl backdrop-blur-xl transition-transform md:static md:translate-x-0 md:shadow-none`}><div className="flex items-center gap-3 border-b border-border pb-7"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-foreground text-sm font-bold text-white">R</span><div><p className="font-bold">ReadyCommerce</p><p className="text-xs text-slate-500">Admin workspace</p></div></div><nav className="mt-8 space-y-2">{links.map((link, index) => <button type="button" key={link.label} onClick={() => setOpen(false)} className={`flex min-h-12 w-full items-center gap-3 rounded-xl px-4 text-left text-sm font-semibold transition ${index === 0 ? 'bg-primary text-white shadow-sm' : 'text-slate-600 hover:bg-muted hover:text-foreground'}`}><span className="w-5 text-center text-base">{link.icon}</span>{link.label}</button>)}</nav></aside>{open && <button type="button" aria-label="Close navigation" onClick={() => setOpen(false)} className="fixed inset-0 z-30 bg-slate-950/30 md:hidden" />}<div className="min-w-0 flex-1"><header className="flex h-20 items-center justify-between border-b border-border bg-white/70 px-5 backdrop-blur-xl sm:px-8"><button type="button" aria-label="Open navigation" onClick={() => setOpen(true)} className="rounded-xl border border-border px-3 py-2 text-lg md:hidden">☰</button><div className="hidden md:block"><p className="text-xs font-bold uppercase tracking-[0.22em] text-primary">Operations</p><p className="mt-1 text-sm text-slate-500">A calm view of your commerce.</p></div><div className="flex items-center gap-3"><span className="hidden text-right sm:block"><span className="block text-sm font-bold">Admin user</span><span className="block text-xs text-slate-500">Administrator</span></span><span className="flex h-10 w-10 items-center justify-center rounded-full bg-primary/10 font-bold text-primary">A</span></div></header><main className="p-5 sm:p-8 lg:p-10">{children}</main></div></div>; }
