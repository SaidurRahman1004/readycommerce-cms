const HomepageConfig=require('../models/HomepageConfig');const Category=require('../models/Category');const {AppError}=require('../middlewares/errorHandler');const get=async(req,res,next)=>{try{const data=await HomepageConfig.findOne({key:'homepage'}).populate('featuredCategories','name slug image').lean();return res.json({success:true,data:data||{slides:[],featuredCategories:[],promoEnabled:false}})}catch(e){return next(e)}};const update=async(req,res,next)=>{try{const ids=Array.isArray(req.body.featuredCategories)?req.body.featuredCategories:[];const valid=await Category.find({_id:{$in:ids}}).distinct('_id');const data={key:'homepage',slides:Array.isArray(req.body.slides)?req.body.slides.slice(0,10):[],featuredCategories:valid,promoEnabled:Boolean(req.body.promoEnabled),promoTitle:String(req.body.promoTitle||'').slice(0,180),promoBody:String(req.body.promoBody||'').slice(0,500)};const doc=await HomepageConfig.findOneAndUpdate({key:'homepage'},{$set:data},{upsert:true,new:true,runValidators:true});return res.json({success:true,data:doc})}catch(e){return next(e)}};module.exports={get,update};
